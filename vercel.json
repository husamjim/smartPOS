{
  "buildCommand": "npm install --prefix frontend --legacy-peer-deps && npm run build:vercel --prefix frontend",
  "outputDirectory": "frontend/dist",
  "cleanUrls": true,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
